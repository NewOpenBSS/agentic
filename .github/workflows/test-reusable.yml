name: Test Reusable
on:
  workflow_call:
    inputs:
      message:
        type: string
        required: false
        default: "hello"
jobs:
  test:
    runs-on: self-hosted
    steps:
      - run: echo "reusable called with message=${{ inputs.message }}"
