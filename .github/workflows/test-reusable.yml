name: Test Reusable Structured
on:
  workflow_call:
    inputs:
      issue_number:
        description: 'Feature issue number'
        required: true
        type: string
      repository:
        description: 'Domain repository'
        required: true
        type: string
jobs:
  test:
    runs-on: self-hosted
    steps:
      - run: echo "issue=${{ inputs.issue_number }} repo=${{ inputs.repository }}"
